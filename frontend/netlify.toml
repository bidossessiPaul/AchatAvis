[build]
  command = "npm run build"
  publish = "dist"

# Legacy file kept for compatibility only.
# No external API redirect here anymore.

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
